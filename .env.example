# Mongo
MONGO_USER=docmgmt
MONGO_PASS=docmgmtpass

# Backend
JWT_SECRET=CHANGE_ME_make_this_at_least_32_characters_long
# 32-byte base64 — generate with: openssl rand -base64 32
MASTER_KEY=CHANGE_ME_openssl_rand_base64_32_output_here==
FRONTEND_ORIGIN=http://localhost:3000

# Frontend
NEXT_PUBLIC_API_BASE=http://localhost:8080/api/v1

# MinIO (dev profile)
MINIO_USER=minio
MINIO_PASS=minio12345
